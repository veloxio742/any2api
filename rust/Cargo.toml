[package]
name = "any2api-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.13.2", default-features = false, features = ["blocking", "rustls", "gzip", "brotli", "deflate"] }
serde_json = "1.0.149"
